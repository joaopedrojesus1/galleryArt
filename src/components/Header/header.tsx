// const links = ["Home", "Art", "About", "Blog"];

export default function Header(){
    return (
        <nav className = "bg-gray-800 flex justify-between p-10">
            <ul></ul>
        </nav>
    )
}